apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: backend-tls
  namespace: default

spec:
  secretName: backend-tls

  dnsNames:
    - deepak-aks-api-2509.centralindia.cloudapp.azure.com

  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer